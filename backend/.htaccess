RewriteEngine On

# 1. If the request is for an actual file (like test.text), serve it
RewriteCond %{DOCUMENT_ROOT}/scandiweb-project/public/%{REQUEST_URI} -f
RewriteRule ^(.*)$ public/$1 [L]

# 2. Everything else (like /graphql) goes to index.php
RewriteRule ^(.*)$ public/index.php [L]